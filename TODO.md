# TODO
- [ ] Fix runtime ReferenceError: `loading` is not defined in `components/dashboard/WelcomeCard.tsx`.
- [ ] Add a proper `loading` prop (or remove the conditional) so `WelcomeCard` always renders safely.
- [ ] Update any call sites if a new prop is required.
- [ ] Run `npm test` / `npm run build` (or `npm run lint`) to verify compilation.

